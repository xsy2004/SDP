% BlackJack

clc
clear


gameLogic("test")


function result = gameLogic(x)
    init = simpleGameEngine('retro_cards.png',160,160,8,[0,100,0]);
    % gamerStatus = 0;
    % defint variable
    empty = 1;
    cards = 0;
    card_sprites = 21:72;
    % make a empty board
    board_display = empty * ones(1,10);
    random_choose = randperm(52);
    % just get number what you need
    pickup = random_choose(1:5)

    for cards = 1:52

    end
    drawScene(init, board_display)
end

